# Desarrollo usa proxy.conf.json para REST y WebSocket same-origin.
# En despliegues separados, backendUrl puede colocarse en public/config.json.
BACKEND_PROXY_TARGET=http://127.0.0.1:8000
API_PREFIX=/api/v1
